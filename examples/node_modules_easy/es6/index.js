/**
 * ExampleNodeModule of es6 in node_modules
 **/
'use strict';

var _debug  = require('debug');

var debug = _debug('easy-babel');

debug("ExampleNodeModule: some frequently used syntax");

debug('ExampleNodeModule: arrow functions');
var a = () => {};

debug('ExampleNodeModule: class');
class B {}

debug('ExampleNodeModule: enhanced objects');
var c = {
    a,
    method () {}
}

debug('ExampleNodeModule: template string');
`how are you`;
`line1
line2`
var tmp1 = 'tmp1';
var tmp2 = 'tmp2';
var d = `One is ${tmp1} and the other is ${tmp2}`;

debug('ExampleNodeModule: destructuring');
var [e, f] = [1, 2, 3];

debug('ExampleNodeModule: default param');
function g (a, b = 3, c) {};

debug('ExampleNodeModule: spread');
var tmp3 = [1, 2, 3];
var h = [...tmp3];

debug('ExampleNodeModule: rest');
var [...i] = [1, 2, 3];

debug('ExampleNodeModule: let');
let j = 1;

debug('ExampleNodeModule: const');
const k = 1;

debug('ExampleNodeModule: for of');
var tmp4 = [1, 2, 3, 4];
for (var l of tmp4) {}

debug('ExampleNodeModule: modules');
import assert from 'assert';

debug('ExampleNodeModule: export');
export default 'export';

debug('ExampleNodeModule: async/await');
async function tmp5 () {};
async function m () { await tmp5(); }
m
